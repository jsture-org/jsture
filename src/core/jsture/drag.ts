import { Jsture, JstureType } from './jsture';

const dragMove: Jsture = {
  type: JstureType.DRAG,
};

const dragStart: Jsture = {
  type: JstureType.DRAG,
};

const dragEnd: Jsture = {
  type: JstureType.DRAG,
};
